machine:
  node:
    version: 0.10.40

dependencies:
  pre:
    - curl https://install.meteor.com | /bin/sh
  override:
    - npm install -g spacejam

test:
  override:
    - spacejam test-packages ./
